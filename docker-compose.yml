services:
  greyline:
    build: .
    ports:
      - "127.0.0.1:5173:5173"
    volumes:
      - ./data:/app/data
    environment:
      - HOST=0.0.0.0
      - PORT=5173
      - NODE_ENV=production
    restart: unless-stopped
